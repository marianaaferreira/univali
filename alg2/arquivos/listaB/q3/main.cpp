#include <iostream>

using namespace std;
/*3. A partir de um arquivo de acesso sequencial DADOS, determine o número da maior linha do arquivo e escreva
os valores numéricos encontrados no arquivo. */

int main(){

    string str;
    ifstream arq;
    while(getline(arq, str)){
        while(cont<str.size())
            if(str[i].isnum())
                cout << str[i];
            if(maior<str.size())
    }
    return 0;
}
